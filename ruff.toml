line-length = 120

extend-exclude = [
  "lm-evaluation-harness",
  "data",
  "output",
  ".venv",
  "venv",
  "__pycache__",
]

[lint]
select = ["E", "F"]
ignore = ["E501"]

[lint.per-file-ignores]
"config/**/*.py" = ["F401"]
